---
title: "Session Events Webhook"
api: "WEBHOOK https://your-app.com/webhooks/multisynq"
description: "Webhook for receiving session events and state changes."
---

This webhook receives real-time events about session changes, user actions, and state updates.

## Webhook Events

Multisynq sends HTTP POST requests to your webhook URL when events occur in your sessions.

### Event Types

- `session.created` - New session created
- `session.deleted` - Session deleted
- `user.joined` - User joined session
- `user.left` - User left session
- `state.changed` - Session state updated
- `connection.lost` - Connection lost (for monitoring)

## Request Headers

<ParamField header="X-Multisynq-Signature" type="string" required>
  HMAC SHA256 signature for webhook verification
</ParamField>

<ParamField header="X-Multisynq-Event" type="string" required>
  The type of event being sent
</ParamField>

<ParamField header="Content-Type" type="string">
  application/json
</ParamField>

## Webhook Payload

<ResponseField name="event" type="string">
  The type of event (session.created, user.joined, etc.)
</ResponseField>

<ResponseField name="sessionId" type="string">
  The unique identifier of the session
</ResponseField>

<ResponseField name="appId" type="string">
  The application ID
</ResponseField>

<ResponseField name="timestamp" type="string">
  ISO timestamp when the event occurred
</ResponseField>

<ResponseField name="data" type="object">
  Event-specific data payload
</ResponseField>

<ResponseExample>

```json User Joined Event
{
  "event": "user.joined",
  "sessionId": "session_abc123",
  "appId": "multisynq_app_456",
  "timestamp": "2024-01-15T10:20:00Z",
  "data": {
    "userId": "user_789",
    "displayName": "John Doe",
    "userAgent": "Mozilla/5.0...",
    "ipAddress": "192.168.1.1"
  }
}
```

```json State Changed Event
{
  "event": "state.changed",
  "sessionId": "session_abc123",
  "appId": "multisynq_app_456",
  "timestamp": "2024-01-15T10:25:00Z",
  "data": {
    "changeset": {
      "document.content": "Updated content",
      "document.lastModified": "2024-01-15T10:25:00Z"
    },
    "userId": "user_789",
    "previousState": {
      "document.content": "Previous content"
    }
  }
}
```

</ResponseExample>

## Webhook Verification

To verify webhook authenticity, compute the HMAC SHA256 signature using your webhook secret:

```javascript
const crypto = require('crypto');

function verifyWebhook(payload, signature, secret) {
  const expectedSignature = crypto
    .createHmac('sha256', secret)
    .update(payload)
    .digest('hex');
  
  return signature === `sha256=${expectedSignature}`;
}
```

## Response

Your webhook endpoint should return a 200 status code to acknowledge receipt. If Multisynq receives a non-200 response, it will retry the webhook delivery up to 3 times with exponential backoff.
