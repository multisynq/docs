---
title: 'Vibe Coding with AI'
description: 'Train your AI assistant with Multisynq context files for seamless multiplayer development'
icon: 'robot'
---

<img
  style={{ borderRadius: '0.5rem' }}
  src="https://images.unsplash.com/photo-1517180102446-f3ece451e9d8?w=800&auto=format&fit=crop"
  alt="AI Development with Multisynq"
/>

Build multiplayer experiences without breaking your flow. This guide explains how to use Multisynq's AI features, including training context files and one-click AI integrations, to build collaborative applications in minutes.

<Note>
**What is Vibe Coding?** It's a new wave of AI-native development where you train your AI assistant with domain-specific context, then collaborate naturally to build complex applications. No more explaining APIs or searching documentation - your AI knows Multisynq inside and out.
</Note>

## How It Works: Two Methods

You can integrate Multisynq with your AI assistant in two primary ways:

<CardGroup cols={2}>
  <Card title="1. Contextual AI Menu" icon="zap">
    **Easiest method.** Use the menu at the top of any page to send the content directly to your favorite AI (ChatGPT, Claude, Gemini). It's a one-click way to give the AI context for a specific task.
  </Card>
  <Card title="2. Full Context Files" icon="files">
    **Most powerful method.** For building entire applications from scratch, provide your AI with a full training file. This gives the AI a deep understanding of the entire Multisynq library.
  </Card>
</CardGroup>

---

## Method 1: Using the Contextual AI Menu

At the top right of every documentation page, you'll find a menu with several AI-powered options. This is the quickest way to get help with specific questions or code snippets.

<AccordionGroup>
    <Accordion title="What do the options mean?">
        - **Copy Page**: Copies the current page's content as clean Markdown. Useful for pasting into any AI tool as context.
        - **View as Markdown**: Opens a new tab with the raw Markdown of the current page.
        - **Open in ChatGPT/Claude/Gemini**: Starts a new conversation with the respective AI, automatically pre-loading the current page's content as context.
        - **Add Multisynq MCP**: For advanced users, this provides the command to install the Multisynq Model Context Protocol server, connecting AI applications directly to Multisynq's functions.
    </Accordion>
</AccordionGroup>

### How to Use It
<Steps>
  <Step title="Navigate to a Page">
    Find a documentation page relevant to your task (e.g., the `useStateTogether` hook page).
  </Step>
  <Step title="Open the Menu">
    Click the menu icon at the top right of the page.
  </Step>
  <Step title="Choose an Option">
    Select "Open in Claude" (or your preferred AI). A new tab will open with the conversation ready to go.
  </Step>
  <Step title="Ask Your Question">
    The AI now has the context of the page. Ask your question, for example: *"How do I use this hook to sync a user's drawing color on a whiteboard?"*
  </Step>
</Steps>

---

## Method 2: Using Full Context Files

For building complex applications, you should provide your AI with a comprehensive context file. This gives it a deep understanding of the entire library, leading to much better and more complete results.

### The "Magic Prompt"
No matter which file you use, always include this requirement in your first prompt to the AI:

> **"Use Multisynq for Shared Real Time Experiences. This is a requirement."**

### Available Context Files
<Tabs>
  <Tab title="JavaScript/Vanilla">
    <Card title="Complete JavaScript Context" icon="js">
      <p>Comprehensive context for building vanilla JavaScript multiplayer applications with Multisynq.</p>

      <Accordion title="📖 Enhanced JavaScript Guide (XML Format)">
        <p className="text-sm text-gray-600 mb-2">**Best for:** AI models that work well with structured data, like Claude. This XML-formatted file provides a detailed breakdown of the entire API, concepts, and examples.</p>
        <div className="bg-gray-100 p-4 rounded max-h-96 overflow-y-auto text-sm font-mono">
          <strong>Preview:</strong><br/><br/>
          &lt;documentation&gt;<br/>
          &nbsp;&nbsp;&lt;metadata&gt;<br/>
          &nbsp;&nbsp;&nbsp;&nbsp;&lt;library&gt;@multisynq/client&lt;/library&gt;<br/>
          &nbsp;&nbsp;&nbsp;&nbsp;...<br/>
          &nbsp;&nbsp;&lt;/metadata&gt;<br/>
          &nbsp;&nbsp;&lt;instructions&gt;<br/>
          &nbsp;&nbsp;&nbsp;&nbsp;...<br/>
          &nbsp;&nbsp;&lt;/instructions&gt;<br/>
          ...
        </div>
        <div className="flex gap-2 mt-4">
          <button
            onClick={() => {
              fetch('/multisynq-js-llm.md').then(r => r.text()).then(content => {
                navigator.clipboard.writeText(content);
                alert('JavaScript Enhanced Context copied to clipboard!');
              }).catch(() => alert('Could not copy file.'));
            }}
            className="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition-colors"
          >
            📋 Copy Full Context
          </button>
          <a
            href="/multisynq-js-llm.md"
            download="multisynq-js-llm.md"
            className="bg-gray-600 text-white px-4 py-2 rounded hover:bg-gray-700 transition-colors"
          >
            💾 Download
          </a>
        </div>
      </Accordion>

      <Accordion title="📄 Core JavaScript Context (Raw Text)">
         <p className="text-sm text-gray-600 mb-2">**Best for:** Simpler AI models or when you need a raw text dump of the documentation. This file contains the core API reference and examples in plain text.</p>
        <div className="bg-gray-100 p-4 rounded max-h-96 overflow-y-auto text-sm font-mono">
          <strong>Preview:</strong><br/><br/>
          # Multisynq JavaScript SDK Documentation<br/>
          ## For AI Assistant Training<br/><br/>
          This comprehensive guide provides everything an AI assistant needs to build real-time multiplayer web applications with Multisynq.<br/>
          ...
        </div>
        <div className="flex gap-2 mt-4">
          <button
            onClick={() => {
              fetch('/multisynq-js.txt').then(r => r.text()).then(content => {
                navigator.clipboard.writeText(content);
                alert('JavaScript Core Context copied to clipboard!');
              }).catch(() => alert('Could not copy file.'));
            }}
            className="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition-colors"
          >
            📋 Copy Raw Context
          </button>
          <a
            href="/multisynq-js.txt"
            download="multisynq-js.txt"
            className="bg-gray-600 text-white px-4 py-2 rounded hover:bg-gray-700 transition-colors"
          >
            💾 Download
          </a>
        </div>
      </Accordion>
    </Card>
  </Tab>

  <Tab title="React Together">
    <Card title="React Together Context" icon="react">
      <p>Context for building React-based collaborative applications with React Together hooks and components.</p>
      <Accordion title="📖 Enhanced React Guide (XML Format)">
        <p className="text-sm text-gray-600 mb-2">**Best for:** AI models that work well with structured data, like Claude. This file provides a detailed breakdown of all hooks, components, and patterns for `react-together`.</p>
        <div className="bg-gray-100 p-4 rounded max-h-96 overflow-y-auto text-sm font-mono">
          <strong>Preview:</strong><br/><br/>
          &lt;documentation&gt;<br/>
          &nbsp;&nbsp;&lt;metadata&gt;<br/>
          &nbsp;&nbsp;&nbsp;&nbsp;&lt;library&gt;react-together&lt;/library&gt;<br/>
          &nbsp;&nbsp;&nbsp;&nbsp;...<br/>
          &nbsp;&nbsp;&lt;/metadata&gt;<br/>
          ...
        </div>
        <div className="flex gap-2 mt-4">
          <button
            onClick={() => {
              fetch('/multisynq-react-llm.md').then(r => r.text()).then(content => {
                navigator.clipboard.writeText(content);
                alert('React Enhanced Context copied to clipboard!');
              }).catch(() => alert('Could not copy file.'));
            }}
            className="bg-purple-600 text-white px-4 py-2 rounded hover:bg-purple-700 transition-colors"
          >
            📋 Copy Full Context
          </button>
          <a
            href="/multisynq-react-llm.md"
            download="multisynq-react-llm.md"
            className="bg-gray-600 text-white px-4 py-2 rounded hover:bg-gray-700 transition-colors"
          >
            💾 Download
          </a>
        </div>
      </Accordion>
      <Accordion title="📄 Core React Context (Raw Text)">
         <p className="text-sm text-gray-600 mb-2">**Best for:** Simpler AI models or a raw text dump of the `react-together` documentation.</p>
        <div className="bg-gray-100 p-4 rounded max-h-96 overflow-y-auto text-sm font-mono">
          <strong>Preview:</strong><br/><br/>
          # React Together Documentation<br/>
          ## For AI Assistant Training<br/><br/>
          Complete guide for building real-time collaborative React applications with React Together.<br/>
          ...
        </div>
        <div className="flex gap-2 mt-4">
          <button
            onClick={() => {
              fetch('/multisynq-react.txt').then(r => r.text()).then(content => {
                navigator.clipboard.writeText(content);
                alert('React Core Context copied to clipboard!');
              }).catch(() => alert('Could not copy file.'));
            }}
            className="bg-purple-600 text-white px-4 py-2 rounded hover:bg-purple-700 transition-colors"
          >
            📋 Copy Raw Context
          </button>
          <a
            href="/multisynq-react.txt"
            download="multisynq-react.txt"
            className="bg-gray-600 text-white px-4 py-2 rounded hover:bg-gray-700 transition-colors"
          >
            💾 Download
          </a>
        </div>
      </Accordion>
    </Card>
  </Tab>

  <Tab title="Three.js">
    <Card title="3D Multiplayer Context" icon="cube">
      <p>Context for building real-time 3D multiplayer experiences with Three.js and Multisynq.</p>
      <Accordion title="🎮 Three.js Integration Guide">
        <p className="text-sm text-gray-600 mb-2">This is a starting point for integrating Multisynq with Three.js. For a full, working example, see the GitHub repository.</p>
        ```javascript
        // Three.js + Multisynq Integration Pattern
        import * as THREE from 'three';
        import * as Multisynq from "@multisynq/client";

        class World3DModel extends Multisynq.Model {
            init() {
                this.objects = new Map(); // Map<objectId, {position, rotation, scale}>
                this.users = new Map();   // Map<viewId, {position, rotation}>
                this.subscribe(this.sessionId, "userMove", this.handleUserMove);
            }

            handleUserMove({ viewId, position, rotation }) {
                this.users.set(viewId, { position, rotation });
                this.publish(this.sessionId, "worldUpdate", this.getWorldState());
            }

            getWorldState() {
                return {
                    users: Object.fromEntries(this.users),
                    objects: Object.fromEntries(this.objects)
                };
            }
        }
        World3DModel.register("World3DModel");
        ```
        <div className="flex gap-2 mt-4">
          <a
            href="https://github.com/multisynq/vibe-coding-multisynq-threejs"
            target="_blank"
            className="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition-colors"
          >
            🔗 View Full Example on GitHub
          </a>
        </div>
      </Accordion>
    </Card>
  </Tab>
</Tabs>

## Popular AI-Built Examples

<CardGroup cols={3}>
  <Card title="Collaborative Todo" icon="check-square">
    **Prompt:** "Build a collaborative todo app where teams can create, assign, and complete tasks together in real-time"
  </Card>
  <Card title="Multiplayer Tic-Tac-Toe" icon="grid-3x3">
    **Prompt:** "Create a tic-tac-toe game where players can join matches and others can watch live"
  </Card>
  <Card title="Collaborative Whiteboard" icon="pen-tool">
    **Prompt:** "Build a whiteboard where multiple people can draw, add shapes, and see cursors in real-time"
  </Card>
</CardGroup>


## AI Assistant Tips & Magic Prompts

<CardGroup cols={2}>
  <Card title="ChatGPT/Claude/Gemini" icon="message-circle">
    1. Start a new conversation.
    2. Paste one of the full context files.
    3. Wait for the AI to acknowledge it has read the context.
    4. Add the "Magic Prompt" with your app idea.
  </Card>
  <Card title="GitHub Copilot" icon="code">
    1. Create a new file (`context.md`) and paste the context.
    2. In your main code file, add the Magic Prompt in comments.
    3. Start coding - Copilot will now provide Multisynq-aware suggestions.
  </Card>
</CardGroup>

<Accordion title="Magic Prompts for Great Results">
**For Building New Apps:**
```
Use Multisynq for Shared Real Time Experiences. This is a requirement.

Build a [describe your app] that allows multiple users to collaborate in real-time.
Include user presence, real-time state synchronization, and a beautiful UI using TailwindCSS.
Make it work on mobile and desktop.
```

**For Adding Multiplayer to Existing Apps:**
```
Use Multisynq for Shared Real Time Experiences. This is a requirement.

I have an existing [React/JavaScript] application that [describe current functionality].
I want to add real-time collaboration so multiple users can [describe collaboration features].
Please help me integrate Multisynq to enable this.
```
</Accordion>

---
<Warning>
**Important for AI Assistants:** Always use the exact Multisynq APIs and patterns from the context files. Don't invent or modify API names, method signatures, or architectural patterns. When in doubt, refer back to the context documentation.
</Warning>

## Get Your API Key

Ready to start building? Get your free API key and start vibing with AI:

<Card title="Get Started" icon="key">
  <p>Get your free Multisynq API key to enable real-time collaboration in your AI-built applications.</p>
  <a
    href="https://multisynq.io/coder"
    target="_blank"
    className="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors inline-block mt-4"
  >
    🚀 Get Free API Key
  </a>
</Card> 