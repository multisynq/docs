---
title: 'ModelRealm'
description: 'API reference for ModelRealm class'
---

# ModelRealm

## Methods

### Instance Methods

#### `register(model)`

**Parameters:**

- `model` (`any`) - 

#### `deregister(model)`

**Parameters:**

- `model` (`any`) - 

#### `publish(event, data, scope)`

**Parameters:**

- `event` (`any`) - 
- `data` (`any`) - 
- `scope` (`any`) - 

#### `subscribe(model, scope, event, methodName)`

**Parameters:**

- `model` (`any`) - 
- `scope` (`any`) - 
- `event` (`any`) - 
- `methodName` (`any`) - 

#### `unsubscribe(model, scope, event, methodName)`

**Parameters:**

- `model` (`any`) - 
- `scope` (`any`) - 
- `event` (`any`) - 
- `methodName` (`any`) - 

#### `unsubscribeAll(model)`

**Parameters:**

- `model` (`any`) - 

#### `future(model, tOffset, methodName, methodArgs)`

**Parameters:**

- `model` (`any`) - 
- `tOffset` (`any`) - 
- `methodName` (`any`) - 
- `methodArgs` (`any`) - 

#### `if(__currentRealm && __currentRealm.equal(this))`

**Parameters:**

- `__currentRealm && __currentRealm.equal(this)` (`any`) - 

#### `cancelFuture(model, methodOrMessage)`

**Parameters:**

- `model` (`any`) - 
- `methodOrMessage` (`any`) - 

#### `if(__currentRealm && __currentRealm.equal(this))`

**Parameters:**

- `__currentRealm && __currentRealm.equal(this)` (`any`) - 

#### `random()`

#### `now()`

#### `equal(otherRealm)`

**Parameters:**

- `otherRealm` (`any`) - 

#### `isViewRealm()`
