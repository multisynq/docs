---
title: 'VMWriter'
description: 'API reference for VMWriter class'
---

# VMWriter

## Methods

### Static Methods

#### `static newOrRecycled(vm)`

**Parameters:**

- `vm` (`any`) - 

#### `static resetInstance()`

### Instance Methods

#### `reusableInstance()`

#### `reusableInstance(val)`

**Parameters:**

- `val` (`any`) - 

#### `addWriter(classId, ClassOrSpec)`

**Parameters:**

- `classId` (`any`) - 
- `ClassOrSpec` (`any`) - 

#### `snapshot(vm)`

@param {VirtualMachine} vm

**Parameters:**

- `vm` (`VirtualMachine`) - 

#### `writeAllStaticInto(state)`

**Parameters:**

- `state` (`any`) - 

#### `writeDeferred()`

#### `write(value, path, defer)`

**Parameters:**

- `value` (`any`) - 
- `path` (`any`) - 
- `defer` (`any`) - 
