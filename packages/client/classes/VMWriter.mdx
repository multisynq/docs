---
title: 'VMWriter'
description: 'API reference for VMWriter class'
---

# VMWriter

## Methods

### Static Methods

#### `static newOrRecycled(vm)`

**Parameters:**

- `vm` (`any`) - 

#### `static resetInstance()`

### Instance Methods

#### `if(!inst)`

**Parameters:**

- `!inst` (`any`) - 

#### `reusableInstance()`

#### `reusableInstance(val)`

**Parameters:**

- `val` (`any`) - 

#### `for(const [classId, ClassOrSpec] of Model.allClassTypes())`

**Parameters:**

- `const [classId` (`any`) - 
- `ClassOrSpec] of Model.allClassTypes()` (`any`) - 

#### `for(const Class of Model.allClasses())`

**Parameters:**

- `const Class of Model.allClasses()` (`any`) - 

#### `if(Object.prototype.hasOwnProperty.call(Class, "okayToIgnore"))`

**Parameters:**

- `Object.prototype.hasOwnProperty.call(Class` (`any`) - 
- `"okayToIgnore")` (`any`) - 

#### `for(const prop of props)`

**Parameters:**

- `const prop of props` (`any`) - 

#### `addWriter(classId, ClassOrSpec)`

**Parameters:**

- `classId` (`any`) - 
- `ClassOrSpec` (`any`) - 

#### `if(!write)`

**Parameters:**

- `!write` (`any`) - 

#### `snapshot(vm)`

@param {VirtualMachine} vm

**Parameters:**

- `vm` (`VirtualMachine`) - 

#### `for(const [key, value] of Object.entries(vm))`

**Parameters:**

- `const [key` (`any`) - 
- `value] of Object.entries(vm)` (`any`) - 

#### `writeAllStaticInto(state)`

**Parameters:**

- `state` (`any`) - 

#### `for(const Class of Model.allClasses())`

**Parameters:**

- `const Class of Model.allClasses()` (`any`) - 

#### `for(const [key, value] of Object.entries(Class))`

**Parameters:**

- `const [key` (`any`) - 
- `value] of Object.entries(Class)` (`any`) - 

#### `for(const [name, ClassOrSpec] of Model.allClassTypes())`

**Parameters:**

- `const [name` (`any`) - 
- `ClassOrSpec] of Model.allClassTypes()` (`any`) - 

#### `if(typeof ClassOrSpec)`

**Parameters:**

- `typeof ClassOrSpec` (`any`) - 

#### `if(writeStatic)`

**Parameters:**

- `writeStatic` (`any`) - 

#### `if(props)`

**Parameters:**

- `props` (`any`) - 

#### `writeDeferred()`

#### `while(index < this.todo.length)`

**Parameters:**

- `index < this.todo.length` (`any`) - 

#### `write(value, path, defer)`

**Parameters:**

- `value` (`any`) - 
- `path` (`any`) - 
- `defer` (`any`) - 

#### `switch(typeof value)`

**Parameters:**

- `typeof value` (`any`) - 
