---
title: 'ViewRealm'
description: 'API reference for ViewRealm class'
---

# ViewRealm

## Methods

### Instance Methods

#### `valid()`

#### `register(view)`

**Parameters:**

- `view` (`any`) - 

#### `deregister(view)`

**Parameters:**

- `view` (`any`) - 

#### `publish(event, data, scope)`

**Parameters:**

- `event` (`any`) - 
- `data` (`any`) - 
- `scope` (`any`) - 

#### `subscribe(event, subscriberId, callback, scope, handling)`

**Parameters:**

- `event` (`any`) - 
- `subscriberId` (`any`) - 
- `callback` (`any`) - 
- `scope` (`any`) - 
- `handling` (`any`) - 

#### `unsubscribe(event, subscriberId, callback, scope)`

**Parameters:**

- `event` (`any`) - 
- `subscriberId` (`any`) - 
- `callback` (`any`) - 
- `scope` (`any`) - 

#### `unsubscribeAll(subscriberId)`

**Parameters:**

- `subscriberId` (`any`) - 

#### `future(view, tOffset)`

**Parameters:**

- `view` (`any`) - 
- `tOffset` (`any`) - 

#### `get(_target, property)`

**Parameters:**

- `_target` (`any`) - 
- `property` (`any`) - 

#### `apply(_method, _this, args)`

**Parameters:**

- `_method` (`any`) - 
- `_this` (`any`) - 
- `args` (`any`) - 

#### `random()`

#### `now()`

#### `externalNow()`

#### `extrapolatedNow()`

#### `isSynced()`

#### `equal(otherRealm)`

**Parameters:**

- `otherRealm` (`any`) - 

#### `isViewRealm()`
