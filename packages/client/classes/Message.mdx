---
title: 'Message'
description: 'API reference for Message class'
---

# Message

## Methods

### Static Methods

#### `static fromState(state, vm)`

**Parameters:**

- `state` (`any`) - 
- `vm` (`any`) - 

### Instance Methods

#### `before(other)`

**Parameters:**

- `other` (`any`) - 

#### `hasReceiver(id)`

**Parameters:**

- `id` (`any`) - 

#### `isExternal()`

#### `externalSeq()`

#### `externalSeq(seq)`

**Parameters:**

- `seq` (`any`) - 

#### `internalSeq()`

#### `internalSeq(seq)`

**Parameters:**

- `seq` (`any`) - 

#### `asState()`

#### `executeOn(vm, nested)`

**Parameters:**

- `vm` (`any`) - 
- `nested` (`any`) - 

#### `shortString()`

#### `toString()`
