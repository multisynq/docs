---
title: 'VMHasher'
description: 'API reference for VMHasher class'
---

# VMHasher

## Methods

### Instance Methods

#### `addHasher(classId, ClassOrSpec)`

**Parameters:**

- `classId` (`any`) - 
- `ClassOrSpec` (`any`) - 

#### `getHash(vm)`

@param {VirtualMachine} vm

**Parameters:**

- `vm` (`VirtualMachine`) - 

#### `hashDeferred()`

#### `hash(value, defer)`

**Parameters:**

- `value` (`any`) - 
- `defer` (`any`) - 

#### `hashModel(model)`

**Parameters:**

- `model` (`any`) - 

#### `hashObject(object, defer)`

**Parameters:**

- `object` (`any`) - 
- `defer` (`any`) - 

#### `hashArray(array, defer)`

**Parameters:**

- `array` (`any`) - 
- `defer` (`any`) - 

#### `hashIntArray(array)`

**Parameters:**

- `array` (`any`) - 

#### `hashStructure(object, value, defer)`

**Parameters:**

- `object` (`any`) - 
- `value` (`any`) - 
- `defer` (`any`) - 

#### `hashEntry(key, value, defer)`

**Parameters:**

- `key` (`any`) - 
- `value` (`any`) - 
- `defer` (`any`) - 
