---
title: 'VMHasher'
description: 'API reference for VMHasher class'
---

# VMHasher

## Methods

### Instance Methods

#### `for(const [classId, ClassOrSpec] of Model.allClassTypes())`

**Parameters:**

- `const [classId` (`any`) - 
- `ClassOrSpec] of Model.allClassTypes()` (`any`) - 

#### `addHasher(classId, ClassOrSpec)`

**Parameters:**

- `classId` (`any`) - 
- `ClassOrSpec` (`any`) - 

#### `getHash(vm)`

@param {VirtualMachine} vm

**Parameters:**

- `vm` (`VirtualMachine`) - 

#### `for(const [key, value] of Object.entries(vm))`

**Parameters:**

- `const [key` (`any`) - 
- `value] of Object.entries(vm)` (`any`) - 

#### `if(key)`

**Parameters:**

- `key` (`any`) - 

#### `hashDeferred()`

#### `while(index < this.todo.length)`

**Parameters:**

- `index < this.todo.length` (`any`) - 

#### `hash(value, defer)`

**Parameters:**

- `value` (`any`) - 
- `defer` (`any`) - 

#### `switch(typeof value)`

**Parameters:**

- `typeof value` (`any`) - 

#### `while(value !)`

**Parameters:**

- `value !` (`any`) - 

#### `if(this.hashers.has(value.constructor))`

**Parameters:**

- `this.hashers.has(value.constructor)` (`any`) - 

#### `if(this.hashers.has(type))`

**Parameters:**

- `this.hashers.has(type)` (`any`) - 

#### `switch(type)`

**Parameters:**

- `type` (`any`) - 

#### `hashModel(model)`

**Parameters:**

- `model` (`any`) - 

#### `for(const [key, value] of Object.entries(model))`

**Parameters:**

- `const [key` (`any`) - 
- `value] of Object.entries(model)` (`any`) - 

#### `hashObject(object, defer)`

**Parameters:**

- `object` (`any`) - 
- `defer` (`any`) - 

#### `for(const [key, value] of Object.entries(object))`

**Parameters:**

- `const [key` (`any`) - 
- `value] of Object.entries(object)` (`any`) - 

#### `hashArray(array, defer)`

**Parameters:**

- `array` (`any`) - 
- `defer` (`any`) - 

#### `for(let i)`

**Parameters:**

- `let i` (`any`) - 

#### `hashIntArray(array)`

**Parameters:**

- `array` (`any`) - 

#### `for(let i)`

**Parameters:**

- `let i` (`any`) - 

#### `hashStructure(object, value, defer)`

**Parameters:**

- `object` (`any`) - 
- `value` (`any`) - 
- `defer` (`any`) - 

#### `hashEntry(key, value, defer)`

**Parameters:**

- `key` (`any`) - 
- `value` (`any`) - 
- `defer` (`any`) - 

#### `if(defer && typeof value)`

**Parameters:**

- `defer && typeof value` (`any`) - 
