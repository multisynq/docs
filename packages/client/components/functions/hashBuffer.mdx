export default function hashBuffer_Component() {
  return (
    <div className="api-function-component">
      <h2 id="hashbuffer">hashBuffer</h2>
      <div className="description">
        <p>return buffer hashed into 256 bits encoded using base64 (suitable in URL)</p>
      </div>
    </div>
  );
}