export default function hashString_Component() {
  return (
    <div className="api-function-component">
      <h2 id="hashstring">hashString</h2>
      <div className="description">
        <p>return string hashed into 256 bits encoded using base64 (suitable in URL)</p>
      </div>
    </div>
  );
}