export default function inSequence_Component() {
  return (
    <div className="api-function-component">
      <h2 id="insequence">inSequence</h2>
      <div className="description">
        <p>Answer true if seqA comes before seqB: - sequence numbers are 32 bit unsigned ints with overflow - seqA comes before seqB if it takes fewer increments to go from seqA to seqB (zero increments counts) than going from seqB to seqA</p>
      </div>
    </div>
  );
}