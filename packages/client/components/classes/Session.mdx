export default function Session_Component() {
  return (
    <div className="api-class-component">
      <h2 id="session">Session</h2>
      <Note>
        This class should not be instantiated directly using <code>new</code>.
      </Note>
      <Tabs>
      </Tabs>
    </div>
  );
}