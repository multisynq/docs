export default function FakeVM_Component() {
  return (
    <div className="api-class-component">
      <h2 id="fakevm">FakeVM</h2>
      <div className="description">
        <p>A fake VM is used to run bit-identical code outside of the model, e.g. for Constant init</p>
      </div>
      <Tabs>
      </Tabs>
    </div>
  );
}